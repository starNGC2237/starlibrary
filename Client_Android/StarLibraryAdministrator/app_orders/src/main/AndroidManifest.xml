<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.e.app_orders">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".OrderDirectlyActivity"></activity>
        <activity android:name=".OrderBorrowInActivity" />
        <activity android:name=".OrderBorrowOutActivity" />
        <activity android:name=".OrderSweepedActivity" />
        <activity android:name=".OrderBlocksShowActivity" />
        <activity android:name=".OrderReadyShowActivity" />
        <activity android:name=".CustomCaptureAActivity" />
    </application>

</manifest>