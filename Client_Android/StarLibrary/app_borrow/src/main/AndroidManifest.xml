<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.e.app_borrow">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".OrderDirectlyActivity" />
        <activity android:name=".TimeOutActivity" />
        <activity android:name=".BorrowedActivity" />
        <activity android:name=".ReservingActivity" />
        <activity android:name=".OrderActivity" />
        <activity android:name=".FavoritesActivity" />
        <activity android:name=".BorrowingBorrowActivity" />
    </application>

</manifest>