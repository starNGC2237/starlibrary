<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".NickNameUsersActivity">
    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbar_nickname"
        android:layout_width="match_parent"
        android:layout_height="?actionBarSize"
        android:layout_alignParentTop="true"
        android:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar"
        app:popupTheme="@style/ThemeOverlay.AppCompat.Light"
        android:background="?attr/colorPrimary">
        <TextView
            android:id="@+id/toolbar_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:text="@string/changeNickName"
            android:textColor="#ffffff"
            android:textSize="18sp"/>
        <Button
            android:id="@+id/users_nickname_save"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/users_save"
            android:layout_gravity="end"
            android:layout_marginEnd="5dp"/>
    </androidx.appcompat.widget.Toolbar>
    <EditText
        android:id="@+id/users_nickname_nicknameED"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/toolbar_nickname"
        android:textSize="20sp"
        android:textColor="#000000"
        android:layout_marginEnd="5dp"
        android:layout_marginStart="5dp"
        android:layout_marginTop="15dp"
        android:lines="1"
        android:ellipsize="end"/>
</RelativeLayout>