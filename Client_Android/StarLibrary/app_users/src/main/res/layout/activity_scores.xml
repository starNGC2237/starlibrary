<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ScoresActivity">
    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbar_scores"
        android:layout_width="match_parent"
        android:layout_height="?actionBarSize"
        android:layout_alignParentTop="true"
        android:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar"
        app:popupTheme="@style/ThemeOverlay.AppCompat.Light"
        android:background="?attr/colorPrimary">
        <TextView
            android:id="@+id/toolbar_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:text="信誉分数"
            android:textColor="#ffffff"
            android:textSize="18sp"/>
        <ImageView
            android:id="@+id/help_tips_img"
            android:layout_width="22dp"
            android:layout_height="22dp"
            android:src="@drawable/help_tips"
            android:layout_gravity="end|center_vertical"
            android:layout_marginEnd="20dp"/>
    </androidx.appcompat.widget.Toolbar>
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/toolbar_scores">
        <TextView
            android:id="@+id/scores_scores"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="90"
            android:textColor="@color/green"
            android:textSize="100sp"
            android:layout_above="@+id/scores_tips_scores"
            android:layout_centerHorizontal="true"/>
        <TextView
            android:id="@+id/scores_tips_scores"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="信誉分数不错，继续保持"
            android:layout_marginTop="20dp"
            android:layout_centerInParent="true"/>
    </RelativeLayout>
</RelativeLayout>